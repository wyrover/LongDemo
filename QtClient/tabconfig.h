#ifndef TABCONFIG_H
#define TABCONFIG_H
#include<QDialog>

class TabConfig : public QDialog
{
    Q_OBJECT
public:
    TabConfig(QWidget *parent = 0);
    ~TabConfig();
};

#endif // TABCONFIG_H
